# Keep Windows launchers as CRLF BYTES in the repo.
# GitHub ZIP downloads do not apply eol smudge filters; LF-only .bat
# breaks cmd.exe ('ocal' / 'd' / 'rshell' is not recognized).
*.bat -text
*.cmd -text
*.ps1 -text

# Unix shell scripts stay LF
*.sh text eol=lf
